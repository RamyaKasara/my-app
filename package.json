{
  "name": "my-app",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "devDependencies": {
    "@types/bun": "latest",
    "concurrently": "^9.2.1"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "workspaces": [
    "packages/*"
  ],
  "_comment": "all directories inside packages should be treated as workspaces",
  "scripts": {
    "dev": "bun run index.ts"
  }
}
